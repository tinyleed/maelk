# The browser has no Supabase auth configuration.
# Configure auth only on the same-origin Express API; see ../api/.env.example.
# Optional: human-readable local UI label only. It is not an auth/security input.
VITE_MAELK_APP_ENV=local
